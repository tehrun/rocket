# Rocket

A minimal Rust project scaffold.

## Commands

Build the project:

```sh
cargo build
```

Run the test suite:

```sh
cargo test
```

Run Clippy lints:

```sh
cargo clippy
```

Format the code:

```sh
cargo fmt
```
